#include <stdio.h>


/*	Psuedo - code

	we do this recursively 
	
	will need to do n! times to get through all options
	
	only need 1  sbub function
	
	make sure when you do something, undo it when you do the next recursiv step
	
	permute function
		if (password length == empty spot)	{		// base case
				printf ("%d. " ++permuter number);
				for (i = 0; i < passwordlength; i++)	{
					print setletters[i]
				print newline)
				
				rerurn
		for i < password length)	{
			if (usedletters[i] == false)	{
				usedletters[i] = trus;
				setletters[emptyspot] = (letters)[i];
				permute(usedletters, setletters, passwordlength, emptyspot + 1)		//recursove step	
				
				usedleters[i] = False
				setletters[emptyspot] = '\0'
			


*/


int main ()	{
	
	
	
}